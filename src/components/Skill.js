// import dependencies
import React, { useEffect, useRef } from 'react';
import styled from 'styled-components';

// define styled components



// define Skill component
function Skill({ logoSrc, text }) {
  
  return (

  );
}

export default Skill;
